---
description: VITE_* is public; no payment or private keys
globs: **/.env*,**/*.{ts,tsx,yml}
alwaysApply: false
---

# Secrets

- `VITE_*` is public. No payment/private keys in env (AC-004).
- Document public keys only in `.env.example`. Gitignore real `.env`.

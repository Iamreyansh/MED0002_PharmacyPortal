---
name: gsap-utils
description: GSAP utility helpers. Use when interpolating or distributing motion values.
---

# GSAP utils

Prefer CSS variables already in `app.css` over JS interpolation for color. Use utils for stagger only.

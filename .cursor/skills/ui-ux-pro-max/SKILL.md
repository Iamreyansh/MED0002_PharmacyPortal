---
name: ui-ux-pro-max
description: Wrapper that tells the agent to use the personal ui-ux-pro-max skill plus pharmacy-dense-SaaS constraints. Use when doing UI/UX work.
---

# ui-ux-pro-max (project wrapper)

Use the personal skill at `~/.cursor/skills/ui-ux-pro-max` (run its `search.py --design-system`). Do not copy its CSV database into this repo.

Pharmacy constraints:

- Product type: dense pharmacy SaaS console, not a marketing landing page.
- Keep existing `app.css` tokens. Do not invent a parallel palette.
- India: ₹, IST, high-density tables.
- Accessibility over novelty.

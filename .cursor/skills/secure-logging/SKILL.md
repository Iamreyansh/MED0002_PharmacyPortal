---
name: secure-logging
description: console and track() deny-list. Use when adding logs, toasts, or telemetry.
---

# Secure logging

Deny-list: token, authorization, refresh, password, pin, otp, aadhaar, rx.

`api_error` telemetry may include `code` only.

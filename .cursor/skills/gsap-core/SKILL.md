---
name: gsap-core
description: GSAP core usage for later motion work. Use when adding GSAP animations. Do not install GSAP unless asked.
---

# GSAP core

Not installed. When added: `gsap` + context-safe tweens, kill on unmount. No inline scripts. Pair with `csp-trusted-types`.

---
name: gsap-react
description: GSAP in React 18. Use when wiring tweens to host components.
---

# GSAP React

Use `useGSAP` or `gsap.context` in `useLayoutEffect`. Revert on unmount. Do not tween from render. Keep React 18.3.1.
